spring:
  application:
    name: order-service-test
  
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
    show-sql: false
aws:
    region: eu-central-1
    sqs:
      queue-url: disabled   # sem chamadas reais em testes
services:
  user:
    url: http://localhost:8081
  product:
    url: http://localhost:8082

